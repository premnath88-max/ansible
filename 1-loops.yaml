- name: demo on loops
  hosts: local
  connection: local
  tasks:
  - name: Print names
    ansible.builtin.debug:
      msg: "Hello {{ item }}"
    loop:
    - Prem
    - Koti
    - Rasm
    - Aruna
    - Hanvika  