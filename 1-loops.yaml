- name: Install packages
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: Intall package
    ansible.builtin.package:
      name: "Installing {{ item }}"
      state: present
    loop:
    - nginx
    - zip
    - mysql  