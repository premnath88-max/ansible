- name: gathering facts
  hosts: local
  connection: local
  tasks:
  - name: print facts
    ansible.builtin.debug:
      msg: "{{ ansible_facts }}"

- name: Check RedHat or not
  ansible.builtin.debug:
    msg: "Yes,Package management is RedHat"
  when: ansible_facts.os_family == "RedHat"

  